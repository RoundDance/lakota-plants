// The public address of the site. It is baked into the QR codes (npm run qr)
// and into the sitemap, so change it here before printing placards if the
// Netlify site name ever changes.
export const SITE_URL = 'https://lakota-plants.netlify.app';
